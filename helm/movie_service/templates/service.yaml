apiVersion: v1
kind: Service
metadata:
  name: {{ include "movie_service.fullname" . }}
  labels:
    {{- include "movie_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "movie_service.selectorLabels" . | nindent 4 }}
