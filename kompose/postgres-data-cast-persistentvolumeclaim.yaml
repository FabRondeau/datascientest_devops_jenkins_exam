apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    lb-service: postgres-data-cast
  name: postgres-data-cast
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
